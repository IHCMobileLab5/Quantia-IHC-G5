{
  "name": "quantia",
  "version": "1.0.0",
  "scripts": {
    "api": "json-server --watch server/db.json --routes server/routes.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}

